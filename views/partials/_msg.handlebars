{{!-- Displays dive with {{success_msg}} if success_msg is valid --}}
{{#if success_msg}}
    <div class="alert aler-success">{{success_msg}}</div>
{{/if}}

{{!-- Displays dive wih {{error_msg}} if error_msg is valid --}} 
{{#if error_msg}}
    <div class="alert alert-danger">{{error_msg}}</div>
{{/if}}